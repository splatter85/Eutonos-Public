# Current State

Initialized by ToVA Project Workflow generation 2 on {{DATE}}.

## Confirmed

- Project name: {{PROJECT_NAME}}
- Repository classification at installation: {{PROJECT_CLASSIFICATION}}
- Existing product and architecture files were preserved.
- Active work belongs in `{{OWNER_currentTask}}`; collision state belongs in `{{OWNER_activeAgentWork}}`.
- Verification commands, if discovered, live in `{{OWNER_projectHealth}}`.
- First-run discovery is routed through `{{OWNER_projectDiscovery}}` only while project definition remains open.
- Complete deployment, initialization, acceptance, and upgrade guidance lives in `{{OWNER_tovaSetup}}` and leaves routine boot after setup.
- Architecture, current/future capabilities, and current/future features have separate owners in `{{OWNER_docsMap}}`.

## Needs Project Review

- Follow `{{OWNER_tovaSetup}}` through full-core evaluation, target-local acceptance, and finalization.
- Run the five-question first-run discovery in `{{OWNER_projectDiscovery}}`, using repository evidence and labeled options where answers are unknown.
- Confirm the product goal and measurable success criteria in `{{OWNER_goals}}`.
- Confirm the durable source, generated-output, data, and integration boundaries in `{{OWNER_architecture}}`.
- Confirm supported outcomes in `{{OWNER_currentCapabilities}}`, concrete mechanisms in `{{OWNER_currentFeatures}}`, and honest future scope in `{{OWNER_futureCapabilities}}` / `{{OWNER_futureFeatures}}`.
- Run the declared health gate before claiming the initialized workflow is accepted.

Completed history belongs to Git and `{{OWNER_docChangeLog}}`, not this compact state file.
