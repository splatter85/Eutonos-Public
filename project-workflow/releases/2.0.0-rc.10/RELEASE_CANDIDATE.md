# ToVA Project Workflow 2.0.0-rc.10

Status: ready for owner release decision after automated TOVA12 evidence is current.

## What This Candidate Adds

- one explicit mature-repository migration doctrine: the current kit is the target workflow baseline while project truth survives;
- release-specific `UPGRADE.json` and `UPGRADE.md` for the supported rc.9 to rc.10 path;
- a human and machine migration ledger with section-level dispositions, extension contracts, and retirement preconditions;
- five installer stages: discover, plan, bootstrap/reconcile, retire, and accept/finalize;
- a read-only migration checker and recorded migration evidence in finalized installation identity;
- semantic role suggestions that never silently become authority;
- exact project-root scoping and nested independent-project exclusion;
- conditional Roadmap, acceptance workspace, capability maturity, and app release-acceptance templates;
- mature F5-F7 fixtures and adversarial zero-write retirement boundaries in addition to F1-F4.

## Decision Boundary

This packet does not approve the release, commit Git changes, publish a package, accept a portable baseline, or install ToVA into another repository. Formulator OS and ATLAS remain unchanged. Their TOVA12.12 read-only reassessment is the next separate campaign action after this HQ candidate.

## Evidence

- `TOVA12_RELEASE_CHECK.json` and `.md` own exact mirror, checksum, identity, script, archive, retirement, and upgrade-contract results.
- `TOVA12.9_MATURE_REPOSITORY_FIXTURE_ACCEPTANCE.json` and `.md` own the disposable F1-F7 proof when generated.
- `.tova/last-verify-report.json` owns the final HQ Project Health result.

## Known Limits

The migration ledger records accepted meaning but the checker does not infer project meaning. Semantic candidates remain suggestions. Optional extension templates remain conditional. The deterministic Workflow Controller and external mature-repository reassessment are outside this candidate's completed implementation boundary.
