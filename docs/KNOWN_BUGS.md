# Known Bugs

Record reproducible package defects that should survive beyond the current task. States are `open`, `investigating`, `blocked`, `deferred`, and `fixed`.

For each bug include a stable ID, severity, area, symptoms, reproduction, expected and actual behavior, likely source owner, evidence, workaround, and verification path. Repair package source rather than disposable fixture output. Mark fixed only after the focused reproduction and required Project Health gate pass; retain root cause and regression risk.

## Open Bugs

No confirmed release-candidate implementation bug is currently recorded.

## Acceptance Boundary

Separate owner release review remains pending. That is an acceptance gate, not a product defect, and is tracked in `docs/CURRENT_TASK.md`.
