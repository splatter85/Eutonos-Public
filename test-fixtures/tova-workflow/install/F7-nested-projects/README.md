# Parent Portfolio Application

This parent application shares one Git repository with an independent tooling project under `Tools/ATLAS`. ToVA discovery must scope itself to the explicit parent root and leave the nested project's owners and installation bytes untouched.
