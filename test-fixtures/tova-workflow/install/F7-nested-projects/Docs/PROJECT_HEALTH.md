# Project Health

Run `npm run test` for focused parent behavior and `npm run build` for package assembly. Nested tooling has an independent health owner and is outside this project's acceptance boundary.
