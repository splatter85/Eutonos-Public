# Current Task

## Workflow Upgrade

- [ ] Reconcile the older ToVA workflow into the current canonical baseline while preserving the project-specific release gate and avoiding feature work during migration.

The application baseline is stable; this checklist owns only the reviewed workflow upgrade.
