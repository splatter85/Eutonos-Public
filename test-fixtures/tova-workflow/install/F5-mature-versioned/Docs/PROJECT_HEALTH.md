# Project Health

Run `npm run test` for focused ledger behavior and `npm run build` for the local package boundary. Final product release acceptance remains separately owned by `Docs/RELEASE_READINESS.md`.
