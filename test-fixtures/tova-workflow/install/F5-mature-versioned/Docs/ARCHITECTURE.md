# Architecture

The application exposes a small deterministic ledger module under `src/`. Repository documentation owns product requirements, verification policy, current work, and final release acceptance as separate responsibilities.
