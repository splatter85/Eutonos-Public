# Project Health

Run `npm run test` for event-card behavior and `npm run build` for deterministic static output. Connector access, publication, and live calendar calls are not part of these local checks.
