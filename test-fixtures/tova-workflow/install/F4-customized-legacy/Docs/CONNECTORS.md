# Connector Boundary

Calendar import is a read-only project adapter. It may normalize owner-approved local fixture data but must not authenticate, publish events, or call a live calendar during local workflow checks.
