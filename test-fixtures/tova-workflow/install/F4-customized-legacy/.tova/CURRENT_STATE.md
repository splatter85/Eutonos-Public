# Current State

The event catalog and static site are operational with synthetic fixture events. Calendar import remains read-only, and public publication requires a separate owner decision after content review.
