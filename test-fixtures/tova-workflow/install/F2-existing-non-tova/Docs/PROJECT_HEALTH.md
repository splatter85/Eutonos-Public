# Project Health

Use `npm run test` for focused behavior checks and `npm run build` to prove the distributable export script still assembles. Both commands are declared in the project package and require no network access.
