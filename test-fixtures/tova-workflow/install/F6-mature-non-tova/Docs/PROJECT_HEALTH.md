# Project Health

Run `npm run test` for focused operations behavior and `npm run build` for package assembly. Adoption must preserve these real commands and must label external operator review as separately pending.
