# Execution Receipts

This optional historical directory retains compact receipts for completed or handed-off runs when they provide recovery or evidence value.

Receipts are not routine boot context, current execution authority, acceptance, or a replacement for Git. Start from `docs/templates/EXECUTION_RECEIPT_TEMPLATE.json`; record only safe metadata and checks that actually ran.
